export type User = {
    userId: number;          // Corresponding to Integer userId
    username: string;        // Corresponding to String username
    password: string;        // Corresponding to String password
    firstname: string;       // Corresponding to String firstname
    surname: string;         // Corresponding to String surname
    description: string;     // Corresponding to String description
  }
  